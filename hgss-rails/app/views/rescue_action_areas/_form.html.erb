<%= simple_form_for(@rescue_action_area) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :rescue_action %>
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
