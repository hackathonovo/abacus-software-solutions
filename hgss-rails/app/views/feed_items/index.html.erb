<h2>Obavijesti</h2>

<table>
  <tr>
    <th>Poslano</th>
    <th>Sadržaj</th>
    <th>Autor</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @feed_items.each do |feed_item| %>
  <tr>
    <td><%= feed_item.created_at %></td>
    <td><%= feed_item.text %></td>
    <td><%= feed_item.author %></td>
    <td><%= link_to 'Prikaži', [feed_item.rescue_action, feed_item] %></td>
    <td><%= link_to 'Uredi', edit_rescue_action_feed_item_path(feed_item.rescue_action, feed_item) %></td>
    <td><%= link_to 'Izbriši', [feed_item.rescue_action, feed_item], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Feed item', new_rescue_action_feed_item_path(params[:rescue_action_id]) %>
